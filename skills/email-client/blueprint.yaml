# Canonical contract for email-client.
# This file was migrated from legacy skill metadata.
# Tighten the placeholder script interfaces as each script contract becomes clearer.

category: automation

# Bump this major version whenever the exported contract changes in a
# breaking way for dependent skills.
interface_version: 1

cross_platform: false

depends_on:
  {}

suggested_permissions:
  bash:
    - command: ["scripts/email-send.sh"]
      reason: "Migrated from legacy permissions.json."
  network:
    []

skill_interface:
  inputs:
    - "User requests that match the SKILL.md description for this skill."
  outputs:
    - "Responses, artifacts, or side effects described by this skill's workflow."
  side_effects:
    - "May invoke local scripts or dependencies as described in SKILL.md."

script_interfaces:
  scripts-email-get-message-id:
    id: scripts-email-get-message-id
    cwd: skill_root
    command: ["scripts/email-get-message-id.sh"]
    interface:
      default:
        min_positionals: 0
        allow_extra_positionals: true
        allow_stdin: false
        notes: "Auto-migrated placeholder interface for scripts/email-get-message-id.sh; tighten this contract when needed."
    exported_interface: []
  scripts-email-send:
    id: scripts-email-send
    cwd: skill_root
    command: ["scripts/email-send.sh"]
    interface:
      default:
        min_positionals: 0
        allow_extra_positionals: true
        allow_stdin: false
        notes: "Auto-migrated placeholder interface for scripts/email-send.sh; tighten this contract when needed."
    exported_interface: []
