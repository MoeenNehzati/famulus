content:
- __init__\.py
- _oauth_tokens\.py
dependencies: []
description: Package support source for email-client OAuth token handling. Each Gmail
  nickname treats a present credential_file as authoritative and fails invalid file
  bindings without fallback; credential_id and legacy OAuth records remain compatibility
  paths only when it is absent. It exposes no caller-facing interface.
gateway:
  language: Python
  path: __init__.py
id: email-client._rtx.source.rtx-init
interfaces: {}
node_type: behavioral_source
schema_version: 6
uses_interfaces:
- interface: credentials.interface.secret-store
  version: 1
- interface: credentials.interface.google
  version: 1
version: 1
