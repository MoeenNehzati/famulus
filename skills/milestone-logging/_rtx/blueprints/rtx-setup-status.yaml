content: [_setup_status_interface\.py]
dependencies:
- blueprint: {base: repository-root, path: src/officina/common/blueprints/famulus-paths-get.yaml}
  reason: Resolve the sole logging directory whose existence determines setup status.
  source: common.source.famulus-paths-get
  version: 1
description: Reports whether the getter-projected milestone logging directory exists.
gateway: {language: Python, path: _setup_status_interface.py}
id: milestone-logging._rtx.source.rtx-setup-status
interfaces:
  milestone-logging._rtx.source.rtx-setup-status.interface.setup-status:
    content: [_setup_status_interface\.py]
    uses_interfaces:
    - {interface: common.interface.famulus-paths-get, version: 1}
    contract:
      arguments: {}
      caller_warnings: []
      direct_io:
        network: []
        reads:
        - {access: read, content: logging directory existence and type, formats: [directory], id: logging-directory-state, medium: local-filesystem, path: <getter-projected-logging-path>, path_match: exact, sensitivity: user-private, system: filesystem}
        writes:
        - {access: write, content: exact setup status object, formats: [json], id: result-write, medium: stdout, sensitivity: derived-private, system: agent-runtime}
      execution:
        consistency: {live: The result reflects the getter-projected path at verification time.}
        lifecycle: finite
        state_effect: read-only
        verification: [{method: output-schema, output_ref: result}]
      helpers: []
      interaction: {mode: unattended}
      outcomes:
      - {id: completed, class: success, outputs: [result], effects: [], caller_action: Accept setup only when set_up is true.}
      - {id: failed, class: error, outputs: [], effects: [], caller_action: Treat setup as unverified and resolve the getter failure.}
      outputs:
      - id: result
        audience: machine
        description: One JSON object containing only the set_up boolean.
        direct_io_ref: result-write
        cardinality: {minimum: 1, maximum: 1}
        ordering: stable
        pagination: {kind: none}
        truncation: {kind: none}
        empty: Never empty on success.
        type: {kind: string, sensitivity: derived-private, format: {named: json}}
      preconditions: []
    description: Return exactly whether the getter-projected logging directory is set up.
    process_binding:
      entry: Interface
      kind: process
      patterns: [{allow_stdin: false, min_positionals: 0, max_positionals: 0}]
    usage: ''
    version: 1
node_type: behavioral_source
platform_support: {linux: true, macos: true, windows: true}
runtime_dependencies: []
schema_version: 6
uses_interfaces:
- {interface: common.interface.famulus-paths-get, version: 1}
version: 1
maturity: stable
