authority:
  owns_filesystem: []
  suggested_permissions:
    bash:
    - command:
      - git
      reason: Suggested approval for project initialization.
    - command:
      - python3
      - -m
      - officina.runtime.python_machine_interface_runner
      - _host_links.py
      - Interface
      reason: Creates host compatibility alias symlinks in the newly scaffolded project
        directory only; no writes outside it, no network.
    network: []
children: {}
content:
- _claude_compat_symlink\.py
- _host_links\.py
- tests/.*
- __init__\.py
description: Code module for the initialize-tdd skill.
exports:
  initialize-tdd._rtx.interface.setup-compat-aliases:
    access:
      allow_all_modules: false
      allowed_callers:
      - initialize-tdd
    source_interface: initialize-tdd._rtx.source.rtx-host-links.interface.setup-compat-aliases
gateway:
  language: Python>=3.11
  path: __init__.py
id: initialize-tdd._rtx
namespace_exports: {}
node_type: module
schema_version: 6
sources:
  initialize-tdd._rtx.source.rtx-host-links:
    blueprint:
      base: module-root
      path: blueprints/rtx-host-links.yaml
version: 1
maturity: stable
