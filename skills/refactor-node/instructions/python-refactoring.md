# Python Refactoring

Use the materialized Python standard closure supplied by the router. Scoped
repository instructions remain authoritative when more specific.

Extend the router map with affected machine-visible identifiers or dry-run plans
and focused or reverse integration evidence. A structural fixture supports a
behavioral claim only after the canonical schema or loader accepts it.

Audit all applicable rule assertions and guidance. Use python-ood.* items for
Python-specific diagnosis and moves without displacing applicable base-node or
module requirements; cite standard IDs and file evidence. Use returned checks,
tests, and assurances for their declared coverage, perform remaining
semantic-review work explicitly, and report missing remedies rather than
inventing policy.

## Report

Return the completed preservation map, diagnosed OOD pressures, Python
invariants, the proposed move, and focused and reverse evidence. The router
owns approval, mutation order, diff inspection, and stop-on-failure behavior.

On approved re-entry, apply exactly the approved move within this Python
partition, run the selected verification, and return the exact diff and
verification evidence to the router.
