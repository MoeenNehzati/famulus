category: skill-making-development-assistant
role: meta-skill
kind: auditor
suggested_permissions:
  bash:
  - command:
    - python3
    - -m
    - officina.runtime.python_machine_interface_runner
    - _rtx/_audit_certifier.py:Interface
    reason: Run mechanical checks, compute audit hashes, and write audit records.
  network: []
skill_interface:
  inputs:
  - User request to certify audit records for one or more installed skills.
  - Optional installed skill names or exact skill root paths.
  outputs:
  - Certification report listing written audit records or the first failed gate.
  side_effects:
  - Runs blueprint sync checks, validators, and tests.
  - Writes .last_audit.json under certified target skill roots.
interfaces:
  machine:
    certify:
      version: 1
      uses_interfaces:
      - interface: skill-drift.machine.compute-hashes
        version: 1
      - interface: skill-drift.machine.drift-status
        version: 1
      - interface: skill-maker.machine.sync-blueprints
        version: 1
      description: Run audit gates and write fresh audit records for selected installed
        skills or all observed blueprint-backed installed skills.
      usage: certify [target ...] [--json]
      allow_all_skills: false
      allowed_callers: []
      patterns:
      - min_positionals: 1
        allow_stdin: false
        allowed_flags:
        - --json
      dependencies:
      - kind: python-package
        name: PyYAML
        reason: Parses skill blueprint files during semantic exactness checks.
      direct_io:
        reads: []
        writes: []
        network: []
      owns_filesystem: []
      invocation:
        kind: python_machine_interface
        entrypoint: _rtx/_audit_certifier.py:Interface
        behavior_sources: []
  llm:
    default:
      version: 1
      description: Primary LLM-facing skill instructions.
      binding:
        kind: skill_file
        path: SKILL.md
      direct_io:
        reads: []
        writes: []
        network: []
      owns_filesystem: []
      allow_all_skills: true
      behavior_sources: []
