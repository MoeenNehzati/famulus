content:
- _job_utils\.py
dependencies:
- blueprint:
    base: module-root
    path: blueprints/rtx-init.yaml
  reason: Loads Python package support from _rtx/__init__.py.
  source: recurring-tasks-rtx.source.rtx-init
  version: 1
description: Exposes the no-argument compatibility interface while retaining the internal
  jobs.yaml enabled-field helper.
gateway:
  language: Python
  path: _job_utils.py
id: recurring-tasks-rtx.source.rtx-job-utils
interfaces:
  recurring-tasks-rtx.source.rtx-job-utils.interface.scripts-job-utils:
    contract:
      arguments: {}
      caller_warnings: []
      direct_io:
        network: []
        reads:
        - access: read
          content: invocation
          formats:
          - text
          id: read-1
          medium: local-system
          sensitivity: public
          system: process
        writes:
        - access: write
          content: diagnostic-report
          formats:
          - text
          id: write-1
          medium: stderr
          sensitivity: derived-private
      execution:
        consistency:
          snapshot: Validates only the argument vector supplied to this invocation.
        lifecycle: finite
        state_effect: read-only
        verification:
        - direct_io_ref: read-1
          method: direct-io-state
      helpers: []
      interaction:
        mode: unattended
      outcomes:
      - caller_action: Continue after the compatibility probe succeeds.
        class: success
        effects: []
        id: accepted
        outputs: []
      - caller_action: Invoke the compatibility interface without arguments.
        class: error
        effects: []
        id: invalid-arguments
        outputs:
        - diagnostic
      outputs:
      - audience: both
        cardinality:
          maximum: 1
          minimum: 0
        description: Caller-visible warning or error text written to stderr.
        direct_io_ref: write-1
        empty: No stderr is emitted on paths without diagnostics.
        id: diagnostic
        ordering: stable
        pagination:
          kind: none
        truncation:
          kind: none
        type:
          kind: string
          sensitivity: derived-private
      preconditions: []
    description: Validate the legacy no-argument compatibility surface without changing
      job state.
    process_binding:
      entry: Interface
      kind: process
      patterns:
      - max_positionals: 0
        min_positionals: 0
        name: owner
    version: 1
node_type: behavioral_source
platform_support:
  linux: true
  macos: false
  windows: false
runtime_dependencies: []
schema_version: 5
uses_interfaces: []
version: 1
