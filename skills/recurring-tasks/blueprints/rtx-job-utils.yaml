content:
- _rtx/_job_utils\.py
dependencies:
- blueprint:
    base: module-root
    path: blueprints/rtx-init.yaml
  reason: Loads Python package support from _rtx/__init__.py.
  source: recurring-tasks.source.rtx-init
  version: 1
description: Exposes the no-argument compatibility interface while retaining the internal jobs.yaml enabled-field helper.
gateway:
  language: Python
  path: _rtx/_job_utils.py
id: recurring-tasks.source.rtx-job-utils
interfaces:
  recurring-tasks.source.rtx-job-utils.interface.scripts-job-utils:
    contract:
      arguments: {}
      preconditions: []
      interaction: {mode: unattended}
      caller_warnings: []
      outputs:
      - {id: diagnostic, audience: both, description: Caller-visible warning or error text written to stderr., direct_io_ref: write-1, cardinality: {minimum: 0, maximum: 1}, ordering: stable, pagination: {kind: none}, truncation: {kind: none}, empty: No stderr is emitted on paths without diagnostics., type: {kind: string, sensitivity: derived-private}}
      outcomes:
      - {id: accepted, class: success, outputs: [], effects: [], caller_action: Continue after the compatibility probe succeeds.}
      - {id: invalid-arguments, class: error, outputs: [diagnostic], effects: [], caller_action: Invoke the compatibility interface without arguments.}
      execution:
        state_effect: read-only
        lifecycle: finite
        consistency: {snapshot: Validates only the argument vector supplied to this invocation.}
        verification: [{method: direct-io-state, direct_io_ref: read-1}]
      helpers: []
      direct_io:
        network: []
        reads:
        - {access: read, content: invocation, formats: [text], id: read-1, medium: local-system, sensitivity: public, system: process}
        writes:
        - {access: write, content: diagnostic-report, formats: [text], id: write-1, medium: stderr, sensitivity: derived-private}
    description: Validate the legacy no-argument compatibility surface without changing job state.
    process_binding:
      entry: Interface
      kind: process
      patterns:
      - max_positionals: 0
        min_positionals: 0
        name: owner
    version: 1
node_type: behavioral_source
platform_support:
  linux: true
  macos: false
  windows: false
runtime_dependencies: []
schema_version: 4
uses_interfaces: []
version: 1
