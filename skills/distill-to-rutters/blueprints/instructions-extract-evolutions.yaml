content: [instructions/extract\-evolutions\.md]
dependencies: []
description: Specifies assignment-closed transparent graphs with exactly one valid successor per declared outcome and production foreign-key closure for Rutters, Voyages, workflows, obligations, and coordinators.
gateway: {language: Markdown, path: instructions/extract-evolutions.md}
id: distill-to-rutters.source.extract-evolutions
interfaces:
  distill-to-rutters.source.extract-evolutions.interface.extract-evolutions:
    content: [instructions/extract\-evolutions\.md]
    uses_interfaces: []
    contract:
      arguments:
        request: {description: 'Input, approved preceding artifacts, live repository context, and corrections.', required: true, sensitivity: user-private, type: {kind: string, sensitivity: user-private}}
      caller_warnings: [{external-side-effect: Creates or revises 03_evolutions_and_transitions.md.}]
      direct_io:
        network: []
        reads:
        - {id: source, medium: repository-filesystem, access: read, content: 'Common root input, approved assignment-ready part, Rutter, Voyage, workflow, obligation, and coordinator identities, owning declarations, current public Rutter exports, tests, and diff', formats: [markdown, yaml, json, python, git-metadata, text], sensitivity: user-private}
        writes:
        - {id: artifact, medium: local-filesystem, access: write, content: 03_evolutions_and_transitions.md stage artifact, formats: [markdown], path: '<input-directory>/<source-stem>_distillation/03_evolutions_and_transitions.md', path_match: exact, sensitivity: derived-private, system: filesystem}
        - {id: response, medium: prompt, access: write, content: Artifact path and validation request, formats: [markdown], sensitivity: derived-private, system: agent-runtime}
      execution:
        consistency: {snapshot: 'Every consulted file is read once; graph foreign keys close against one approved assignment chain, each declared outcome has exactly one valid successor, and all consulted paths are rehashed before acceptance.'}
        effects:
        - id: artifact-write
          direct_io_ref: artifact
          action: update
          value_source: {kind: argument, argument_ref: request}
          may_occur_in_outcomes: [graph-ready, graph-gap, partial, failed]
          confirmation_evidence: {kind: direct-io, direct_io_ref: artifact}
          reversibility: {reversible: Delete or revise this unapproved artifact without changing approved preceding artifacts.}
        lifecycle: finite
        mutation_safety:
          atomicity: {per_effect_only: Only the current stage artifact is written.}
          concurrent_invocations: {unsafe: Concurrent revisions can overwrite the same artifact.}
          idempotency: {idempotent: Unchanged inputs and correction authority produce the same artifact content.}
          on_uncertain_completion: {verify_then_decide: Read the artifact before retrying.}
          partial_effects_on_failure: {possible: The artifact may be written before the response is returned.}
          rollback_on_failure: {available: Remove only the incomplete current artifact.}
        state_effect: mutating
        verification: [{method: direct-io-state, direct_io_ref: artifact}]
      helpers: []
      interaction: {mode: interactive, channel: tty, unattended_outcome: approval-required}
      outcomes:
      - {id: graph-ready, class: success, outputs: [artifact, response], effects: [artifact-write], caller_action: Validate this artifact before authorizing logic validation.}
      - {id: graph-gap, class: partial, outputs: [artifact, response], effects: [artifact-write], caller_action: Validate the gap artifact before a graph repair.}
      - {id: partial, class: partial, outputs: [artifact, response], effects: [artifact-write], caller_action: Resolve incomplete graph work without advancing.}
      - {id: approval-required, class: refusal, outputs: [response], effects: [], caller_action: Supply the missing stage authority or requested correction.}
      - {id: failed, class: error, outputs: [artifact, response], effects: [artifact-write], caller_action: Resolve the recorded failure without advancing.}
      outputs:
      - {id: artifact, audience: both, description: 03_evolutions_and_transitions.md stage artifact., direct_io_ref: artifact, cardinality: {minimum: 0, maximum: 1}, ordering: stable, pagination: {kind: none}, truncation: {kind: none}, empty: Empty only on refusal or failure., type: {kind: string, sensitivity: derived-private}}
      - {id: response, audience: human, description: 'Artifact path, validation request, or gap.', direct_io_ref: response, cardinality: {minimum: 1, maximum: 1}, ordering: stable, pagination: {kind: none}, truncation: {kind: none}, empty: Never empty., type: {kind: string, sensitivity: derived-private}}
      preconditions: []
    description: Extract assignment-closed evolutions, Voyage membership, coordinator rules, actor-owned decisions, validators, outcomes, and a deterministic transition matrix with valid evolution or terminal targets.
    usage: <input-markdown> <approved-breakdown> <approved-assignment> [correction]
    version: 1
node_type: behavioral_source
schema_version: 6
uses_interfaces: []
version: 1
maturity: experimental
