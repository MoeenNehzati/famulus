authority:
  owns_filesystem: []
  suggested_permissions:
    bash:
    - args_prefix:
      - --caller-skill
      - find-handoff-candidates
      - find-handoff-candidates._rtx.interface.scan
      command:
      - dispatcher
      reason: Read-only scan of local session transcript files; no writes, no network.
    - args_prefix:
      - --caller-skill
      - find-handoff-candidates
      - find-handoff-candidates._rtx.interface.calibrate
      command:
      - dispatcher
      reason: Read-only diagnostic tool for re-deriving default threshold reference
        numbers; no writes, no network.
    network: []
children: {}
content:
- _handoff_calibrate\.py
- _handoff_scan\.py
- __init__\.py
- _claude_parser\.py
- _codex_parser\.py
- tests/.*
description: Code module for the find-handoff-candidates skill.
exports:
  find-handoff-candidates._rtx.interface.calibrate:
    access:
      allow_all_modules: false
      allowed_callers:
      - find-handoff-candidates
    source_interface: find-handoff-candidates._rtx.source.rtx-handoff-calibrate.interface.calibrate
  find-handoff-candidates._rtx.interface.scan:
    access:
      allow_all_modules: true
      allowed_callers: []
    source_interface: find-handoff-candidates._rtx.source.rtx-handoff-scan.interface.scan
gateway:
  language: Python>=3.11
  path: __init__.py
id: find-handoff-candidates._rtx
namespace_exports: {}
node_type: module
schema_version: 6
sources:
  find-handoff-candidates._rtx.source.rtx-handoff-calibrate:
    blueprint:
      base: module-root
      path: blueprints/rtx-handoff-calibrate.yaml
  find-handoff-candidates._rtx.source.rtx-handoff-scan:
    blueprint:
      base: module-root
      path: blueprints/rtx-handoff-scan.yaml
  find-handoff-candidates._rtx.source.rtx-init:
    blueprint:
      base: module-root
      path: blueprints/rtx-init.yaml
version: 1
maturity: stable
