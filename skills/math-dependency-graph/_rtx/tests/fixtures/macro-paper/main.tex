\documentclass{localbold}
\newcommand{\R}{\mathbb{R}}
\DeclareMathOperator{\ev}{ev}
\input{defs}

\begin{document}
Let $\ev_x:\R^d\to\R$ be an evaluation map with normal $\BFn$ and gradient
$\BFnabla f$. The sequence is $x_1,\dots,x_n$ and its sum is $\sum_i x_i$.

\newcommand{\MidMacro}{\QTC\circ\ev}
\input{nested/more}

$\OuterMacro{A}{B}$ and $\MidMacro$ are used here.
\end{document}
