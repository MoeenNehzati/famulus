category: research-assistant
role: document-processing
kind: builder
suggested_permissions:
  bash:
  - command:
    - latexmk
    reason: Suggested approval for LaTeX Workshop-compatible builds.
  - command:
    - rg
    reason: Suggested approval for LaTeX Workshop-compatible builds.
  network: []
skill_interface:
  inputs:
  - User requests that match the SKILL.md description for this skill.
  outputs:
  - Responses, artifacts, or side effects described by this skill's workflow.
  side_effects:
  - May invoke local scripts or dependencies as described in SKILL.md.
interfaces:
  machine: {}
  llm:
    default:
      version: 1
      description: Primary LLM-facing skill instructions.
      binding:
        kind: skill_file
        path: SKILL.md
      direct_io:
        reads: []
        writes: []
        network: []
      owns_filesystem: []
      allow_all_skills: true
      behavior_sources: []
