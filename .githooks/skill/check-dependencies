#!/usr/bin/env bash
# Guideline v6 — dependencies: every exact cross-module skill-name mention in a
# SKILL.md body must be represented by the owning source `uses_interfaces`.
# Local references: parent paths in SKILL.md may only point to ../references/,
# ../../tools/, or ../../scripts/.
set -uo pipefail

repo_root="$(git rev-parse --show-toplevel)"
python3 "$repo_root/repo_checks.py" --suite validators --repository-view staged --validator skill-maker/dependencies
