#!/usr/bin/env bash
# Repository-validation owner for the live v4 module/source graph, tracked
# blueprint files, ownership paths, and generated contract synchronization.
# Certification and migration remain separate gates and are not duplicated here.
set -uo pipefail

repo_root="$(git rev-parse --show-toplevel)"
python3 "$repo_root/skills/skill-maker/validators/blueprints.py"
