#!/usr/bin/env bash
# Repository-validation owner for tracked blueprint files and ownership paths.
# Target v3 machine-module shape is owned by the JSON schemas; admissibility,
# certification, and migration are separate gates and are not duplicated here.
set -uo pipefail

repo_root="$(git rev-parse --show-toplevel)"
python3 "$repo_root/skills/skill-maker/validators/blueprints.py"
