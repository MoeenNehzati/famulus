# Ephemeral runtime data
history.jsonl
sessions/
session-env/
cache/
paste-cache/
shell-snapshots/
file-history/
backups/
downloads/
tasks/
projects/
ide/
mcp-needs-auth-cache.json

# Local/machine-specific settings
settings.local.json
settings.json

# Skill runtime state
skills/email-triage/state/
skills/email-triage/triage.log

# recurring-tasks: generated at setup time, not source-controlled
skills/recurring-tasks/scripts/runners/
skills/recurring-tasks/scripts/env.sh
skills/recurring-tasks/logs/

# Credentials and runtime state
.credentials.json
.last-cleanup
.last-update-result.json

# Upstream plugin cache (not owned by us)
plugins/

# Python cache/build artifacts
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
**/*.egg-info/
_build/

# Stray local copies of cloud-backed lists (source of truth is Drive, not the
# skill dir). Leaving them here tempts edits to the wrong file. Keep blueprint.
skills/list-manager/*.yaml
!skills/list-manager/blueprint.yaml

*.code-workspace
