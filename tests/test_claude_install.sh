#!/usr/bin/env bash
set -euo pipefail

cat <<'EOF'
Placeholder: Claude install visibility test is not implemented yet.

Intended coverage:
- load this repository as a Claude plugin in an isolated environment
- verify every skills/*/SKILL.md entry is visible as an installed skill
- verify shared references are packaged and path-resolvable

Skipped for now because the local Claude subscription is unavailable.
EOF
