{
  "session_start": "## Skill interfaces\nFor `BEGIN BLUEPRINT INTERFACES`, do not invoke private scripts directly. Call `Executable Interfaces` through the `famulus_dispatcher` MCP server using invocation metadata and `Arguments JSON`. `Instruction Interfaces` are LLM-readable instructions; follow them directly.\n\nFor an executable interface marked `Security level: x`, invoke only `famulus_dispatcher.invoke_security_x` with its `caller`, `interface`, `version`, and `Arguments JSON`; never use a different security-tier tool.",
  "mcp_tool": "famulus_dispatcher.invoke_security_<x>",
  "record": "Persist each evaluator-selected payload verbatim, then submit that exact object to the packaged MCP ClientSession.call_tool path.",
  "cases": [
    {
      "case_id": "T3C-A",
      "skill": "skills/milestone-logging/SKILL.md",
      "user_task": "List the available milestone sessions."
    },
    {
      "case_id": "T3C-B",
      "skill": "skills/milestone-logging/SKILL.md",
      "user_task": "Record the progress message 'Task 3 evaluator replay' with role task-3-evaluator and return the record path."
    },
    {
      "case_id": "T3C-C",
      "skill": "skills/loose-mode/SKILL.md",
      "user_task": "Loose mode: outline three possible ways to reduce cold-start latency in a Python CLI, labeling uncertainty."
    }
  ]
}
