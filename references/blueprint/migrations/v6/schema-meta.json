{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema-meta.json",
  "title": "Version 6 Blueprint Schema Metadata",
  "description": "Metadata marker for the isolated direct-routing migration schema.",
  "type": "object",
  "required": ["$schema", "$id", "title", "description"],
  "properties": {
    "$schema": {"type": "string"},
    "$id": {"type": "string", "minLength": 1},
    "title": {"type": "string", "minLength": 1},
    "description": {"type": "string", "minLength": 1}
  }
}
