schema_version: 6
id: recurring.source.runtime
version: 1
maturity: experimental
node_type: behavioral_source
description: Validates the recurring-owned descriptor and renders one shared native scheduler set.
gateway: {language: Python, path: runtime.py}
content: [runtime\.py, native\.py, records\.py]
dependencies: []
uses_interfaces:
- {interface: launchers.interface.agent, version: 1}
interfaces:
  recurring.source.runtime.interface.python-api:
    version: 1
    description: Build, persist, and validate recurring-owned schedule authority and native artifacts.
    content: [runtime\.py, native\.py, records\.py]
    uses_interfaces:
    - {interface: launchers.interface.agent, version: 1}
    contract:
      arguments:
        operation: {description: Runtime validation rendering or record operation., required: true, sensitivity: public, type: {kind: string}}
        canonical-python: {description: Canonical absolute executable selected by the Task 2 fingerprint., required: false, sensitivity: user-private, type: {kind: string, sensitivity: user-private}}
        plugin-root: {description: Current selected Famulus plugin root., required: false, sensitivity: user-private, type: {kind: string, sensitivity: user-private}}
        descriptor: {description: Canonical recurring-owned schedule descriptor., required: true, sensitivity: user-private, direct_io_ref: authority, type: {kind: file, syntax: literal, relative_to: caller-cwd, must_exist: true, access: read, opaque_content: true}}
      preconditions: []
      interaction: {mode: unattended}
      caller_warnings: []
      outputs:
      - {id: result, audience: machine, description: Validated schedule or native artifact result., direct_io_ref: result, cardinality: {minimum: 1, maximum: 1}, ordering: stable, pagination: {kind: none}, truncation: {kind: none}, empty: Never empty on success., type: {kind: string, sensitivity: user-private}}
      outcomes:
      - {id: completed, class: success, outputs: [result], effects: [], caller_action: Consume the validated result.}
      execution: {state_effect: read-only, lifecycle: finite, consistency: {live: Descriptor jobs owner and native state are revalidated.}, verification: [{method: output-schema, output_ref: result}]}
      helpers: []
      direct_io:
        network: []
        reads: [{id: authority, medium: local-filesystem, access: read, content: Recurring descriptor jobs registrations owner and records., formats: [json, yaml, files], sensitivity: user-private, path: $famulus-recurring-root/**, path_match: glob}]
        writes: [{id: result, medium: local-system, access: write, content: Validated result., formats: [text], sensitivity: user-private}]
platform_support: {linux: true, macos: true, windows: true}
runtime_dependencies:
- {kind: python-package, name: PyYAML, version: any, platforms: {linux: true, macos: true, windows: true}, reason: Parses canonical recurring jobs configuration.}
