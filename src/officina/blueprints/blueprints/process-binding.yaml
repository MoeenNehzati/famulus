content:
- process_binding\.py
dependencies:
- blueprint:
    base: module-root
    path: blueprints/graph.yaml
  reason: Imports blueprint node and export types from graph.py.
  source: blueprints.source.graph
  version: 1
description: Validates typed or authored process-binding layouts, parses caller argv
  against their declared grammar, and compiles deterministic gateway invocation plans
  without executing them.
gateway:
  language: Python
  path: process_binding.py
id: blueprints.source.process-binding
interfaces: {}
node_type: behavioral_source
schema_version: 6
uses_interfaces: []
version: 1
